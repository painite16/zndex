package code

func ConcatenateStrings(str1, str2 string) string {
	var name = str1
	var surname = str2
	return name + " " + surname
}

//Напишите функцию ConcatenateStrings(str1, str2 string) string, которая принимает две строки и склеивает их вместе, разделяя пробелом.

//Примечания
//Например, если передать функции ConcatenateStrings(str1, str2 string) string строки Ira и Khomyakova, то она должна вернуть строку Ira Khomyakova.
